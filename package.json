{
  "name": "temp-api",
  "version": "1.1.0",
  "main": "index.js",
  "scripts": {
    "build": "node build.js",
    "dev": "node build.js && node --watch index.js"
  },
  "author": "Sagnik Das",
  "license": "ISC",
  "description": "API to get temperature from reading the linux file /sys/class/thermal/thermal_zone2/temp",
  "dependencies": {
    "chart.js": "^4.5.1"
  },
  "devDependencies": {
    "esbuild": "^0.25.12"
  }
}
